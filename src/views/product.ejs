<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details - <%= product.productTitle %></title>
    <style>
        /* ... previous styles ... */
        .product-image {
            max-width: 100%;
            height: auto;
            margin: 20px 0;
        }
        .product-meta {
            color: #666;
            font-size: 0.9em;
            margin: 10px 0;
        }
        .buy-button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1.1em;
        }
        .buy-button:hover {
            background-color: #45a049;
        }
    </style>
</head>

<body>
    <a href="/products" class="back-link">← Back to Products</a>
    
    <% if (product) { %>
        <div class="product-details">
            <h1><%= product.productTitle %></h1>
            
            <% if (product.image) { %>
                <img src="<%= product.image %>" alt="<%= product.productTitle %>" class="product-image">
            <% } %>
            
            <p><%= product.productDescription %></p>
            
            <div class="product-meta">
                <% if (product.category) { %>
                    <p>Category: <%= product.category %></p>
                <% } %>
                <% if (product.stock) { %>
                    <p>Stock: <%= product.stock %> units</p>
                <% } %>
            </div>
            
            <p class="price">Price: <%= product.productPrice %> Bath</p>
            
            <button class="buy-button" onclick="addToCart('<%= product.id %>')">
                Add to Cart
            </button>
        </div>

        <script>
            function addToCart(productId) {
                // Add to cart logic here
                alert('Product added to cart!');
            }
        </script>
    <% } else { %>
        <div class="error">
            <h1>Product Not Found</h1>
            <p>Sorry, the requested product could not be found.</p>
        </div>
    <% } %>
</body>
</html>